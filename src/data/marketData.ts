import type { MarketItem, MorningAlert, ActionItem, OvernightStat } from '../types';

export const marketItems: MarketItem[] = [];

export const morningAlerts: MorningAlert[] = [];

export const actionItems: ActionItem[] = [];

export const overnightStats: OvernightStat[] = [];

export const marketOpenTime = '08:00';
export const briefGeneratedAt: string | null = null;
export const monitoringStartedAt: string | null = null;
